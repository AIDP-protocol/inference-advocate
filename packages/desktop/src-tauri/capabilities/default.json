{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Default capability for the advocate desktop shell. No remote permissions.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
