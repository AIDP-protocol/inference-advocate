{
  "name": "@aidp/daemon",
  "version": "0.1.0-pre",
  "description": "Local HTTP daemon exposing the advocate core to the UI on 127.0.0.1. No cloud component.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aidp/core": "*"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
